# Проверка выпуска Voxlyra v1.10.1

- Pytest: 138 тестов собрано, 137 успешно, 1 необязательный пропущен.
- Регрессионный тест: восемь одновременных вызовов `init_db()` на старой схеме проходят без дублирования `content_type` и `reading_mode`.
- Дополнительная проверка: два отдельных Python-процесса одновременно обновляют базу v1.9.9 до текущей схемы без ошибок.
- UI audit: 88/88 статических кнопок имеют обработчики.
- Проверка развёртывания: 87/87 при заполненных обязательных переменных.
- Python: `compileall` пройден.
- JavaScript: все файлы `static/js/*.js` прошли `node --check`.
- Пользовательская база и папка `storage` в архив обновления не входят.
