# Финальная проверка VoxLyra v1.11.10

Проверено:

- компиляция Python-модулей — успешно;
- автоматические тесты — 234 успешно, ошибок нет;
- проверка структуры развёртывания — 163 из 163;
- аудит кнопок Mini App — 83 из 83;
- в публикации с обложкой явно передаётся `ParseMode.HTML`;
- в публикации без обложки явно передаётся `ParseMode.HTML`;
- уведомления ручной модерации и завершения проверки явно используют `ParseMode.HTML`;
- пользовательские названия книг и имена продолжают экранироваться перед отправкой.

Не выполнялось в контейнере:

- реальный Redeploy на Bothost;
- отправка тестового поста в живой Telegram-канал;
- исправление уже опубликованных старых сообщений.
