# Итоговая проверка VoxLyra v1.11.9

## Автоматические проверки

- `python -m compileall -q app tests` — успешно;
- `pytest -q` — 231 успешно, 0 ошибок;
- `python scripts/ui_audit.py` — 83 статические кнопки имеют обработчики;
- `python scripts/deploy_check.py` с заполненными тестовыми переменными — 163/163.

## Проверено отдельно

- в классификаторе нет повторяющихся кодов;
- в классификаторе нет повторяющихся видимых названий;
- все 231 жанр доступны через полный алфавитный список;
- рекомендации различаются для романа, руководства и детской иллюстрированной книги;
- теги для тёмного фэнтези отличаются от тегов нон-фикшна;
- предупреждения для хоррора и криминала поднимаются в раздел «Возможные по выбранному»;
- возрастная подсказка повышается до 12+, 16+ или 18+ по чувствительным темам;
- постраничные callback-данные и callback-данные разделов укладываются в лимит Telegram.

## Не проверено в контейнере

- фактическое отображение обновлённых клавиатур в Telegram после Redeploy;
- удобство на конкретной ширине экрана пользователя.
