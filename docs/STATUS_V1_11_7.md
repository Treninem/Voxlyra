# Статус VoxLyra v1.11.7

Готово исправление экрана «Моё», локальных иконок, Telegram-уведомлений, ежедневного бонуса и favicon.

- заголовок, профиль и имя пользователя больше не накладываются;
- подключена фотография профиля Telegram с двумя резервами;
- все 20 иконок включены в обновление и получают актуальную версию кэша;
- теги `<b>` корректно обрабатываются Telegram;
- повторное нажатие бонуса не создаёт traceback;
- бесполезная бонусная кнопка скрыта, накопленные баллы сохранены;
- `/favicon.ico` больше не должен возвращать 404;
- версия владельца: v1.11.7.

База данных и `storage` не сбрасываются. Следующая точка — Redeploy и проверка в живом Telegram WebView.
