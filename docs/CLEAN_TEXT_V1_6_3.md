# v1.6.3 — чистые тексты и восстановление Mini App

Что исправлено:
- Убран текст `CHANNEL_ID` из раздела канала.
- Убраны пользовательские упоминания `WEBAPP_URL`, `BOT_TOKEN` и похожих технических названий.
- Ошибки Mini App переписаны нормальным языком.
- `/health` больше не показывает служебную версию проекта.

После установки:
1. Обновите `PROJECT_VERSION=v1.6.3-clean-text-and-webapp-recovery`.
2. Сделайте Redeploy, не Restart.
3. Проверьте `/health`.
