# Финальная проверка VoxLyra v1.11.0

## Автоматические проверки

- pytest: **191 passed, 1 skipped**;
- UI audit: **87 статических кнопок имеют обработчики**;
- deploy check: **154/154** с тестовыми безопасными переменными;
- Python compile: успешно;
- JavaScript: `app.js` и `control.js` проходят синтаксическую проверку;
- Jinja: **15/15** шаблонов разобраны без ошибок;
- Premium: план, активация, защита от повторной обработки платежа, статистика владельца, TTS-приоритет, стили цитат и интерфейс проверены;
- цены книги и глав: бесплатный режим, продажа только целиком, продажа по главам и диапазоны проверены;
- база: миграции выполняются без сброса существующего содержимого.

## Что проверяется только после Redeploy

- скорость сборки Docker на выбранном тарифе Bothost;
- фактическая загрузка многоголосой модели Vosk из сети при сборке;
- субъективная естественность каждого голоса на реальном художественном тексте;
- отсутствие ограничений автовоспроизведения на конкретной версии Telegram WebView;
- реальный платёж Premium в Telegram Stars и автопродление.

Диагностика владельца показывает фактический TTS-движок и причины остановок. Поэтому после установки нельзя считать старый Piper новым голосом: резервный режим отмечается явно.
