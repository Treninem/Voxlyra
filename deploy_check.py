# Финальный релиз Вокслиры v1.8.1

Версия: `v1.8.1-final-release`.

## Этап 9 из 9

Финальный этап завершает основной план развития платформы.

В релиз включено:

- безопасный аватар бота с крупной золотой V и открытой книгой;
- отдельный аватар канала с микрофоном, пером и звуковой волной;
- отдельные файлы аватаров в корне проекта для простой загрузки в Telegram;
- обновлённая инструкция BotFather и канала;
- финальная версия в активных инструкциях Bothost;
- зависимость HTTP-клиента для полного запуска встроенных сквозных тестов;
- проверки формата и размера релизных изображений;
- итоговая сборка полного проекта, обновления и рабочей памяти.

## Правила обновления на Bothost

1. Сохранить рабочий `.env`.
2. Сохранить `data/voxlyra.sqlite3`.
3. Сохранить содержимое `storage/`.
4. Заменить файлы из архива обновления.
5. Установить `PROJECT_VERSION=v1.8.1-final-release`.
6. Выполнить Redeploy.
7. Проверить `/health`, `/readiness`, `/start`, каталог и скрытое управление владельца.

## Аватары

- `voxlyra_bot_avatar_final.png` загружается через `@BotFather`.
- `voxlyra_channel_avatar_final.png` устанавливается в настройках канала.
- Встроенные копии остаются в `static/img/`.
